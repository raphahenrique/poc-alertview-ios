# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'poc-alertView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for poc-alertView
  pod 'TinyConstraints'

  target 'poc-alertViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'poc-alertViewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
